bootstrapGuidePath: package/bootstrap/guide.md
bootstrapGuideMaxLines: 80
bootstrapGuideRequiredText:
  - Buildr Skill
  - 兜底入口
  - buildr bootstrap guide
  - buildr runtime list --json
  - 识别当前 Agent
  - 支持列表中对应的参数
  - buildr init --agent <agent>
  - buildr skill install <agent>
  - claude-code
  - codex
  - cursor
  - qoder
  - trae
  - trae-work
  - workbuddy
  - buildr sync <agent>
  - 产品能力同步
  - 当前 Agent runtime render
  - buildr doctor --agent <agent>
  - 不要省略 `--agent`
  - buildr init
  - buildr project create
  - --repo
  - buildr service create
  - buildr rules add
  - buildr rules remove
  - 优先使用 Buildr CLI
  - 根据用户目标和 doctor 结果
  - workspace 必须完成
  - Agent runtime 是面向当前 Agent 的可重建入口
  - 组织资产先改变源资产
  - enabled、required 且 installed
  - disabled 或 uninstalled Rule
  - 请联系 Buildr 作者反馈该 Agent
  - buildr component list/check
  - CLI 不根据对象名称猜测 Component 边界
bootstrapGuideForbiddenText:
  - onboarding.contract.yml
  - bootstrap.contract.yml
  - buildr skills add
  - buildr runtime check claude-code

buildrSkillPath: package/targets/runtime/skills/buildr/SKILL.md
buildrSkillMaxLines: 160
buildrSkillRequiredSections:
  - Buildr 是什么
  - 执行循环
  - 任务路由
  - 资产维护
  - Workspace
  - Project
  - Service
  - Rules
  - Commands
  - Skills
  - Agent 运行时渲染
  - 完成标准
buildrSkillRequiredText:
  - 优先使用 Buildr CLI
  - 状态变更后确认最新 doctor 结果
  - 根据用户目标和 doctor 结果
  - 建立事实基线
  - 创建或修复 Project/Service 必须来自用户意图
  - AGENTS.md
  - projects/manifest.yml
  - commands/manifest.yml
  - skills/manifest.yml
  - buildr commands add
  - buildr commands remove
  - buildr commands check
  - buildr rules add
  - buildr rules remove
  - "enabled: true"
  - "state: installed"
  - 不替 Agent 判断 optional Rule
  - buildr runtime list --json
  - 识别当前 Agent
  - 支持列表中对应的参数
  - buildr doctor --agent <agent>
  - 不要省略 `--agent`
  - buildr skills add
  - --remote-source
  - --resolved-source
  - buildr skills remove
  - runtime check
  - 专项 runtime 细查
  - rules render
  - skills render
  - buildr sync
  - buildr update
  - buildr builtin list
  - 请联系 Buildr 作者反馈该 Agent
  - task-worktree
  - task-finish
  - task-cockpit
  - task-asset-review
  - git-ops
  - components/manifest.yml
  - buildr component install
  - buildr component uninstall
  - 二次确认
buildrSkillForbiddenText:
  - onboarding.contract.yml
  - bootstrap.contract.yml
  - openspec-contract-guard
  - buildr openspec
  - --reference
  - "## 轻约束"
  - "## 命令地图"
  - "| 目的 |"
  - "manifest 示例"

globalForbiddenText:
  - buildr bootstrap install-skill
  - buildr onboard
  - buildr use

generatedSkillRequiredText:
  - 当前 Agent Adapter
  - buildr runtime list --json
  - buildr skill install claude-code
  - buildr sync claude-code
  - buildr doctor --agent claude-code --target <dir> --json
  - buildr runtime check claude-code
  - buildr rules render claude-code
  - buildr skills render claude-code
