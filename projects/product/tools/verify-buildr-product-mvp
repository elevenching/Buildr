#!/usr/bin/env bash
set -euo pipefail

product_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
scenario_root="$product_root/tools/verify/mvp"

source "$scenario_root/setup.sh"
source "$scenario_root/runtime-help.sh"
source "$scenario_root/workspace-project.sh"
source "$scenario_root/assets.sh"
source "$scenario_root/services-runtime.sh"
source "$scenario_root/reconciliation-package.sh"
