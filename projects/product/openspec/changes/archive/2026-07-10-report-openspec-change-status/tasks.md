## 1. Skill Contract

- [x] 1.1 扩展随包 `task-triage` Skill，在采用 OpenSpec 时输出当前 change 状态摘要及刷新时机。
- [x] 1.2 更新产品包验证，检查 task-triage 保留 change 状态、进度和下一动作或阻塞原因约束。

## 2. Runtime And Verification

- [x] 2.1 将更新后的 workspace Skill 渲染到当前 Codex runtime，并确认源资产与 runtime 一致。
- [x] 2.2 运行 package check、产品总验证、OpenSpec strict validation、Codex doctor 和 `git diff --check`。
