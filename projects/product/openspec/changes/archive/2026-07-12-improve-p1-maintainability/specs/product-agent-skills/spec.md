## REMOVED Requirements

### Requirement: workspace/project Skills 源资产维护命令
**Reason**: capability 已按资产所有权拆分，避免产品入口 Skill、资产维护与任务工作流继续耦合在单一规范中。

**Migration**: Requirement 同名迁移到 `managed-skill-assets`，行为保持不变。

### Requirement: workspace/project Skills 支持两种类型
**Reason**: capability 已按资产所有权拆分。

**Migration**: Requirement 同名迁移到 `managed-skill-assets`，行为保持不变。

### Requirement: Buildr Skill 前置说明 Skill 类型和安装方式
**Reason**: capability 已按资产所有权拆分。

**Migration**: Requirement 同名迁移到 `managed-skill-assets`，行为保持不变。

### Requirement: Codex Skills runtime 投射
**Reason**: capability 已按资产所有权拆分。

**Migration**: Requirement 同名迁移到 `managed-skill-assets`，行为保持不变。

### Requirement: Skills manifest schemaVersion
**Reason**: capability 已按资产所有权拆分。

**Migration**: Requirement 同名迁移到 `managed-skill-assets`，行为保持不变。

### Requirement: 内置场景化 Skills 引导产品工作流
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: Buildr Skill 引导场景化内置 Skills
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: 任务工作流必须显式可见
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: Buildr Skill 统一表达 doctor 生命周期
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: Buildr 通过声明式 Skill Contribution 编排 OpenSpec 契约门禁
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: task-triage 明确 OpenSpec 中文文档约束
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: Git Ops 默认保持线性任务历史
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: Task worktree 提供 change 单写入与验证证据边界
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: Task Finish 自动编排已验证任务收尾
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。

### Requirement: 实现任务采用分层验证门禁
**Reason**: capability 已按 Agent 任务工作流所有权拆分。

**Migration**: Requirement 同名迁移到 `agent-task-workflows`，行为保持不变。
