name: Verify Buildr

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: verify-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  product:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]
    defaults:
      run:
        working-directory: projects/product
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
          cache-dependency-path: projects/product/package-lock.json
      - name: Install product dependencies
        run: npm ci
      - name: Install supported OpenSpec CLI
        run: npm install --global @fission-ai/openspec@1.4.1
      - name: Verify final product candidate
        env:
          BUILDR_TIMING_OUTPUT: ${{ runner.temp }}/buildr-verification-timing.json
        run: ./tools/verify-buildr-product
      - name: Upload verification timing
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: verification-timing-linux-node-${{ matrix.node }}
          path: ${{ runner.temp }}/buildr-verification-timing.json
          if-no-files-found: warn

  release-smoke:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    defaults:
      run:
        working-directory: projects/product
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: projects/product/package-lock.json
      - name: Install product dependencies
        run: npm ci
      - name: Run unit tests
        run: npm run test:unit
      - name: Verify packed release lifecycle
        run: npm run test:release
